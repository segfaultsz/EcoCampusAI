# Phase 5: Predictions - Context

**Boundary:** Build `/predictions` page with 7-day forecast chart, peak alert cards, model accuracy panel, what-if simulator, explanation panel.

**Components:** ForecastChart (area+line+confidence band), PeakAlertCards grid, ModelAccuracyCard, WhatIfSimulator (sliders), ExplanationPanel.

**Data:** Mock predictions with 7-day hourly forecast, confidence intervals, peak events.
