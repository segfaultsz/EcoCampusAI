---
phase: 08-data-availability-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/app/routers/real_data.py, backend/app/services/data_service.py]
autonomous: true
requirements: [DASH-03, ENRG-03]
must_haves:
  truths:
    - "Backend provides /api/real/energy/hourly endpoint"
    - "Hourly data includes actual kwh, predicted kwh, and temperature"
  artifacts:
    - path: "backend/app/routers/real_data.py"
      provides: "New route /energy/hourly"
---

<objective>
Implement the hourly energy consumption route in the backend to support the Dashboard and Energy Analytics charts.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Implement get_hourly_stats service</name>
  <files>backend/app/services/data_service.py</files>
  <action>
    Add a function `get_hourly_stats(building_id: str)` that:
    1. Fetches historical energy readings for the last 24 hours.
    2. Fetches predictions for the same period (using the forecaster model).
    3. Fetches temperature data (using real_data_service historical_weather).
    4. Aggregates them into the format: [{hour: "HH:00", kwh: number, predicted: number, temp: number}, ...].
  </action>
  <verify>Check service unit test or manual call</verify>
  <done>Service returns aggregated hourly data</done>
</task>

<task type="auto">
  <name>Task 2: Add /energy/hourly route to real_data router</name>
  <files>backend/app/routers/real_data.py</files>
  <action>
    Add a GET endpoint `/energy/hourly` that takes `building` query param and calls the new service.
  </action>
  <verify>curl http://localhost:8000/api/real/energy/hourly?building=ALL</verify>
  <done>Endpoint returns 200 with JSON data</done>
</task>
</tasks>

<success_criteria>
- Backend returns valid hourly data in the format required by the frontend.
</success_criteria>
