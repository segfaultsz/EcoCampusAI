---
phase: 08-data-availability-fixes
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [backend/.env.example, frontend/src/app/api/energy/hourly/route.js, backend/app/services/real_data_service.py]
autonomous: true
requirements: [DASH-01, INT-02]
---

<objective>
Fix AQI data availability and connect the frontend to the new backend hourly route.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Fix AQI Service and Documentation</name>
  <files>backend/app/services/real_data_service.py, backend/.env.example</files>
  <action>
    1. Update `fetch_aqi` to handle potential API format changes from data.gov.in.
    2. Ensure `DATA_GOV_IN_KEY` is correctly read and used.
    3. Update .env.example to emphasize the need for this key.
  </action>
  <verify>curl http://localhost:8000/api/real/aqi/current</verify>
  <done>AQI endpoint returns real data instead of 503 error</done>
</task>

<task type="auto">
  <name>Task 2: Create Frontend Proxy Route for Hourly Energy</name>
  <files>frontend/src/app/api/energy/hourly/route.js</files>
  <action>
    Create a Next.js API route that proxies requests to `http://localhost:8000/api/real/energy/hourly`.
  </action>
  <verify>Navigate to /api/energy/hourly in browser</verify>
  <done>Frontend proxy returns data from backend</done>
</task>
</tasks>

<success_criteria>
- AQI card in dashboard shows live data.
- EnergyLineChart in dashboard renders actual and predicted lines.
</success_criteria>
